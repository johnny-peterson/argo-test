apiVersion: v1
kind: Service
metadata:
  name: {{ include "capoeira-api.fullname" . }}
  labels:
    {{- include "capoeira-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capoeira-api.selectorLabels" . | nindent 4 }}
